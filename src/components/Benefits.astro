---
export interface Props {
  heading: string;
}
const { heading } = Astro.props;
---
<style>
.tilted {
    position: relative;
    width: 100%;
    height: 400px;
    z-index: 0;
    background: rgba(250, 238, 233, 0.1);
}

.tilted:after {
    content: '';
    width: 50%;
    height: 100%;
    position: absolute;
    background: rgba(250, 238, 233, 1);
    z-index: -1;
    top: 0;
    right: 0;
    bottom: 0;
    left: 50%;
    transform-origin: top left;
    transform: skewY(-4deg);
}

.tilted:before {
    content: '';
    width: 50%;
    height: 100%;
    position: absolute;
    background: rgba(250, 238, 233, 1);
    z-index: -1;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    transform-origin: bottom right;
    transform: skewY(4deg);
}
</style>
<article class="tilted flex justify-center">
    <section class="up flex flex-col justify-center items-center">
        <h3 class="text-xl font-medium my-3">{ heading }</h3>
        <p class="max-w-screen-sm mx-3">
            <slot/>
        </p>
    </section>
</article>